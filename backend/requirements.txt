fastapi==0.104.1
uvicorn==0.24.0
websockets==12.0
sqlalchemy==2.0.23
aiofiles==23.2.1
python-multipart==0.0.6
httpx==0.25.2
textblob==0.17.1
vaderSentiment==3.3.2
google-generativeai==0.3.2
pydantic==2.5.0
pydantic-settings==2.1.0
alembic==1.13.1
# HTTP client for Murf API
aiohttp==3.9.1
httpx==0.25.2
# AI/ML libraries (optional - install only if you want to use them)
# Uncomment the lines below if you want enhanced emotion analysis
# vaderSentiment==3.3.2
# textblob==0.17.1
# google-generativeai==0.3.2
# openai==1.3.8
# Text-to-Speech (TTS) Dependencies
pyttsx3>=2.90

# Audio Processing (optional but recommended for MP3 conversion)
pydub>=0.25.1

# Sentiment Analysis Dependencies
vaderSentiment>=3.3.2
textblob>=0.17.1
# Development dependencies
python-dotenv==1.0.0
pytest==7.4.3
pytest-asyncio==0.21.1

# Static file serving
aiofiles==23.2.1

# JSON handling
orjson==3.9.10

# Logging
loguru==0.7.2
# UUID generation
uuid==1.30
# Web requests (for fallback TTS services)
requests>=2.28.0

# Additional audio support
# On Windows, you might also need:
# pywin32>=227  # For Windows TTS voices
# On Linux, you might need:
# espeak-ng  # Install via system package manager: sudo apt install espeak-ng

# For MP3 support with pydub, you'll need ffmpeg:
# Windows: Download from https://ffmpeg.org/
# Linux: sudo apt install ffmpeg
# macOS: brew install ffmpeg